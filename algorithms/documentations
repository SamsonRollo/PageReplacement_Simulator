Superclass PageReplacementAlgorithm is extended by other algorithms
    methods:
        setValues(PageInput)
        move()
        insertFrameAtReference(int, int, String[][])
        pageFault(int, String[][], int)
        alreadyExistInFrame(int, String[][])
        getFrames()
        getPageFaults()

Fifo
    initialize by Fifo(PageInput)
    move by move()
    retrieve the frames by getFrames(), a String[][] type
        rows are the refereces and columns are the frames
    retrieve the number of page fault by getPageFaults(), an int type