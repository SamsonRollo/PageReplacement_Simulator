Fifo
    initialize by Fifo(PageInput)
    move by move()
    retrieve the frames by getFifoFrame(), a String[][] type
        rows are the refereces and columns are the frames
    retrieve the number of page fault by getPageFaults(), an int type